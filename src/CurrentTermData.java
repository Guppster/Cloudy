/**
 * @author: Gurpreet
 */
public class CurrentTermData extends BaseData
{

    public CurrentTermData() {
        super(temp, tempMin, tempMax, windSpeed, windDirection, pressure, iconID, description);
    }
}
